import React from 'react';

type Props = {}

export const Home = (props: Props) => {

    return (
        <div>
            <h1>My Home</h1>
            <p>This is just a simple react site to allow someone to create simple forms with simple json.</p>
            <p>This is merely for practice.  Nothing more.</p>
        </div>

    );
}